export type DefaultThemeFonts = {
	size: {
		large: string;
		medium: string;
		small: string;
	};
};

export const DefaultThemeFonts = {
	size: {
		large: `24px`,
		medium: `16px`, // Default font size.
		small: `14px`,
	},
} as DefaultThemeFonts;
