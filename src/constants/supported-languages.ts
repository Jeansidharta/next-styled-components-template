/**
 * The languages supported by this application. This array should only contain
 * elements that would be valid in a `<html>`'s lang attribute.
 * At least one language should be specified, or the app won't build.

 * A short description of the valid options can be found here:
 * https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes#attr-lang

 * A complete description of the valid options can be found here:
 * https://www.ietf.org/rfc/bcp/bcp47.txt
 */
export const supportedLanguages = ['en-US'] as const;
